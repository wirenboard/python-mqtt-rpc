#compdef mqtt-rpc-client

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _mqtt-rpc-client (e.g. ~/.local/share/zsh_completion/_mqtt-rpc-client).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_wb_common_commands() {
  local _commands=(
    
  )
  _describe 'mqtt-rpc-client commands' _commands
}

_shtab_wb_common_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  {-b,--broker}"[MQTT broker url (default\: unix\:\/\/\/var\/run\/mosquitto\/mosquitto.sock)]:broker_url:"
  {-d,--driver}"[Driver name (default\: None)]:driver:"
  {-s,--service}"[Service name (default\: None)]:service:"
  {-m,--method}"[Method name (default\: None)]:method:"
  {-a,--args}"[Method arguments (default\: \{\})]:args:"
  {-t,--timeout}"[Timeout in seconds (default\: 10)]:timeout:"
)

# guard to ensure default positional specs are added only once per session
_shtab_wb_common_defaults_added=0


_shtab_wb_common() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->mqtt-rpc-client'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_wb_common_defaults_added )); then
    if (( ${_shtab_wb_common_options[(I)${(q)one_or_more}*]} +          ${_shtab_wb_common_options[(I)${(q)remainder}*]} +          ${_shtab_wb_common_options[(I)${(q)default}]} == 0 )); then
      _shtab_wb_common_options+=(': :_shtab_wb_common_commands' '*::: :->mqtt-rpc-client')
    fi
    _shtab_wb_common_defaults_added=1
  fi
  _arguments -C -s $_shtab_wb_common_options

  case $state in
    mqtt-rpc-client)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_wb_common-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_wb_common -N mqtt-rpc-client
else
  # autoload from fpath, call function directly
  _shtab_wb_common "$@"
fi

